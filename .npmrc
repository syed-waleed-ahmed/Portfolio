# Refuse to install if Node version doesn't satisfy `engines.node`.
# Prevents the "works on my machine" Node-version drift class of bugs.
engine-strict=true

# Default audit level is `low` which is mostly noise.
# CI uses --audit-level=high explicitly; this just quiets local installs.
audit-level=moderate
