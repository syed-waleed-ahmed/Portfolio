# Copy this file to frontend/.env (already gitignored) and override values for local dev.
# In production, set these in Netlify's UI — never commit real values.

# Optional: backend URL override. Leave blank in production to use the
# baked-in default (the deployed backend on Render).
VITE_API_BASE_URL=http://localhost:5000
